# 所需的随机数生成器的源
| **编号** | **测试项** | **测试目的** | **测试步骤** | **预期结果** | **实际结果** | 
|--------- | ---------- | ------------ | ------------ | ------------ | ------------ |
| 0010018 | 将有 /dev/random 设备没有 /dev/hwrng 设备的主机添加到仅勾选了**所需的随机数生成器的源：**中的 **/dev/random** 这个设备的集群中。 | 验证主机是不会因为随机数生成器相关问题而添加失败的。 | 1. 选择**集群**选项卡，点击**新建**。<br/> 2. 弹出**新建集群**弹出框，为该集群输入**名称**；选择相应的 **CPU 类型**；在**集群版本**中选择 3.5，此时便可以对**所需的随机数生成器的源**中的设备进行勾选。<br/> 3. 勾选 **/dev/random**。<br/>4. 点击**确定**。<br/>5. 点击**引导操作**，弹出一个**新建集群-引导操作**弹出框。<br/>6. 点击**添加另外一个主机**，弹出**新建主机**。<br/>7. 输入已准备好的有 /dev/random 而没有 /dev/hwrng 设备的主机的名称，地址和密码。<br/>8. 点击**确定**。 | 点击刚刚添加的主机，弹出该主机的子选项卡。选择**事件**，没有报告与随机数生成器相关的错误。 | 
| 0010019 | 将有 /dev/random 设备没有 /dev/hwrng 设备的主机添加到仅勾选了**所需的随机数生成器的源：**中的**/dev/hwrng** 这个设备的集群中。 | 验证主机会因为随机数生成器相关问题而导致添加失败的。 | 1. 选择**集群**选项卡，点击**新建**。<br/> 2. 弹出**新建集群**弹出框，为该集群输入**名称**；选择相应的 **CPU 类型**；在**集群版本**中选择 3.5，此时便可以对**所需的随机数生成器的源**中的设备进行勾选。<br/> 3. 勾选 **/dev/hwrng**。<br/>4. 点击**确定**。<br/>5. 点击**引导操作**，弹出一个**新建集群-引导操作**弹出框。<br/>6. 点击**添加另外一个主机**，弹出**新建主机**。<br/>7. 输入已准备好的有 /dev/random 而没有 /dev/hwrng 设备的主机的名称，地址和密码。<br/>8. 点击**确定**。 | 点击刚刚添加的主机，弹出该主机的子选项卡。选择**事件**，报告与随机数生成器相关的错误：Host xxx does not comply with the cluster xxx Random Number Generator sources. The Hosts supported sources are: RANDOM; and the cluster requirements are: HWRNG. ||
| 0010020 | 将有 /dev/random 设备没有 /dev/hwrng 设备的主机添加到勾选了**所需的随机数生成器的源：**中的**/dev/hwrng** 和 **/dev/hwrng** 这两个设备的集群中。 | 验证主机会因为随机数生成器相关问题而导致添加失败的。 | 1. 选择**集群**选项卡，点击**新建**。<br/> 2. 弹出**新建集群**弹出框，为该集群输入**名称**；选择相应的 **CPU 类型**；在**集群版本**中选择 3.5，此时便可以对**所需的随机数生成器的源**中的设备进行勾选。<br/> 3. 勾选 **/dev/hwrng** 和 **/dev/hwrng** 这两个设备。<br/>4. 点击**确定**。<br/>5. 点击**引导操作**，弹出一个**新建集群-引导操作**弹出框。<br/>6. 点击**添加另外一个主机**，弹出**新建主机**。<br/>7. 输入已准备好的有 /dev/random 而没有 /dev/hwrng 设备的主机的名称，地址和密码。<br/>8. 点击**确定**。 | 点击刚刚添加的主机，弹出该主机的子选项卡。选择**事件**，报告与随机数生成器相关的错误：Host xxx does not comply with the cluster xxx Random Number Generator sources. The Hosts supported sources are: Random; and the cluster requirements are: HWRNG,RANDOM.  |
| 0010021 | 将有 /dev/random 设备没有 /dev/hwrng 设备的主机添加到没有勾选**所需的随机数生成器的源：**中任何一个设备的集群中。 | 验证主机不会因为随机数生成器相关问题而导致添加失败的。 | 1. 选择**集群**选项卡，点击**新建**。<br/> 2. 弹出**新建集群**弹出框，为该集群输入**名称**；选择相应的 **CPU 类型**；在**集群版本**中选择 3.5，此时便可以对**所需的随机数生成器的源**中的设备进行勾选。<br/> 3. 不勾选 **dev/random** 和 **dev/hwrng** 中的任何一个设备。<br/>4. 点击**确定**。<br/>5. 点击**引导操作**，弹出一个**新建集群-引导操作**弹出框。<br/>6. 点击**添加另外一个主机**，弹出**新建主机**。<br/>7. 输入已准备好的有 /dev/random 而没有 /dev/hwrng 设备的主机的名称，地址和密码。<br/>8. 点击**确定**。 | 点击刚刚添加的主机，弹出该主机的子选项卡。选择**事件**，没有报告与随机数生成器相关的错误。 |  |
| 0010022 | 将有 /dev/hwrng 设备没有 /dev/random 设备的主机添加到仅勾选了**所需的随机数生成器的源：**中的 **/dev/random** 这个设备的集群中。 | 验证主机会因为随机数生成器相关问题导致添加失败。 | 1. 选择**集群**选项卡，点击**新建**。<br/> 2. 弹出**新建集群**弹出框，为该集群输入**名称**；选择相应的 **CPU 类型**；在**集群版本**中选择 3.5，此时便可以对**所需的随机数生成器的源**中的设备进行勾选。<br/> 3. 勾选 **/dev/hwrng**。<br/>4. 点击**确定**。<br/>5. 点击**引导操作**，弹出一个**新建集群-引导操作**弹出框。<br/>6. 点击**添加另外一个主机**，弹出**新建主机**。<br/>7. 输入已准备好的有 /dev/random 而没有 /dev/hwrng 设备的主机的名称，地址和密码。<br/>8. 点击**确定**。 | 点击刚刚添加的主机，弹出该主机的子选项卡。选择**事件**，报告与随机数生成器相关的错误：Host xxx does not comply with the cluster xxx Random Number Generator sources. The Hosts supported sources are: HWRNG; and the cluster requirements are: RANDOM. |  |
| 0010023 | 将有 /dev/hwrng 设备没有 /dev/random 设备的主机添加到仅勾选了**所需的随机数生成器的源：**中的 **/dev/hwrng** 这个设备的集群中。 | 验证主机是不会因为随机数生成器相关问题而添加失败的。 | 1. 选择**集群**选项卡，点击**新建**。<br/> 2. 弹出**新建集群**弹出框，为该集群输入**名称**；选择相应的 **CPU 类型**；在**集群版本**中选择 3.5，此时便可以对**所需的随机数生成器的源**中的设备进行勾选。<br/> 3. 勾选 **/dev/hwrng**。<br/>4. 点击**确定**。<br/>5. 点击**引导操作**，弹出一个**新建集群-引导操作**弹出框。<br/>6. 点击**添加另外一个主机**，弹出**新建主机**。<br/>7. 输入已准备好的有 /dev/hwrng 而没有 /dev/random 设备的主机的名称，地址和密码。<br/>8. 点击**确定**。 | 点击刚刚添加的主机，弹出该主机的子选项卡。选择**事件**，没有报告与随机数生成器相关的错误。 | |
| 0010024 | 将有 /dev/hwrng 设备没有 /dev/random 设备的主机添加到勾选了**所需的随机数生成器的源：**中的 **/dev/random** 和 **/dev/hwrng** 这两个设备的集群中。 | 验证主机会因为随机数生成器相关问题导致添加失败。 | 1. 选择**集群**选项卡，点击**新建**。<br/> 2. 弹出**新建集群**弹出框，为该集群输入**名称**；选择相应的 **CPU 类型**；在**集群版本**中选择 3.5，此时便可以对**所需的随机数生>成器的源**中的设备进行勾选。<br/> 3. 勾选 **/dev/random** 和 **/dev/hwrng**。<br/>4. 点击**确定**。<br/>5. 点击**引导操作**，弹出一个**新建集群-引导操作**弹出框。<br/>6. 点击**添加另外一个主机**，弹出**新建主机**。<br/>7. 输入已准备好的有 /dev/hwrng 而没有 /dev/random 设备的主机的名称，地址和密码。<br/>8. 点击**确定**。 | 点击刚刚添加的主机，弹出该主机的子选项卡。选择**事件**，报告>与随机数生成器相关的错误：Host xxx does not comply with the cluster xxx Random Number Generator sources. The Hosts supported sources are: HWRNG; and the cluster requirements are: RANDOM,HWRNG. |
| 0010025 | 将有 /dev/hwrng 设备没有 /dev/random 设备的主机添加到没有勾选**所需的随机数生成器的源：**中任何一个设备的集群中。 | 验证主机不会因为随机数生成器相关问题而导致添加失败的。 | 1. 选择**集群**选项卡，点击**新建**。<br/> 2. 弹出**新建集群**弹出框，为该集群输入**名称**；选择相应的 **CPU 类型**；在**集群版本**中选择 3.5，此时便可以对**所需的随机数生成器的源**中的设备进行勾选。<br/> 3. 不勾选 **dev/random** 和 **dev/hwrng** 中的任何一个设备。<br/>4. 点击**确定**。<br/>5. 点击**引导操作**，弹出一个**新建集群-引导操作**弹出框。<br/>6. 点击**添加另外一个主机**，弹出**新建主机**。<br/>7. 输入已准备好的有 /dev/random 而没有 /dev/hwrng 设备的主机的名称，地址和密码。<br/>8. 点击**确定**。 | 点击刚刚添加的主机，弹出该主机的子选项卡。选择**事件**，没有报告与随机数生成器相关的错误。 |
| 0010026 | 将有 /dev/random 设备和 /dev/hwrng 设备的主机添加到仅勾选了**所需的随机数生成器的源：**中的**/dev/random** 这个设备的集群中。 | 验证主机不会因为随机数生成器相关问题而导致添加主机失败。 | 1. 选择**集群**选项卡，点击**新建**。<br/> 2. 弹出**新建集群**弹出框，为该集群输入**名称**；选择相应的 **CPU 类型**；在**集群版本**中选择 3.5，此时便可以对**所需的随机数生成器的源**中的设备进行勾选。<br/> 3. 勾选 **/dev/random**。<br/>4. 点击**确定**。<br/>5. 点击**引导操作**，弹出一个**新建集群-引导操作**弹出框。<br/>6. 点击**添加另外一个主机**，弹出**新建主机**。<br/>7. 输入已准备好的有 /dev/random 和 /dev/hwrng 两个设备的主机的名称，地址和密码。<br/>8. 点击**确定**。 | 点击刚刚添加的主机，弹出该主机的子选项卡。选择**事件**，没有报告与随机数生成器相关的错误。 |
| 0010027 | 将有 /dev/random 设备和 /dev/hwrng 设备的主机添加到仅勾选了**所需的随机数生成器的源：**中的**/dev/hwrng** 这个设备的集群中。 | 验证主机不会因为随机数生成器相关问题而导致添加主机失败。 | 1. 选择**集群**选项卡，点击**新建**。<br/> 2. 弹出**新建集群**弹出框，为该集群输入**名称**；选择相应的 **CPU 类型**；在**集群版本**中选择 3.5，此时便可以对**所需的随机数生成器的源**中的设备进行勾选。<br/> 3. 勾选 **/dev/hwrng**。<br/>4. 点击**确定**。<br/>5. 点击**引导操作**，弹出一个**新建集群-引导操作**弹出框。<br/>6. 点击**添加另外一个主机**，弹出**新建主机**。<br/>7. 输入已准备好的有 /dev/random 和 /dev/hwrng 两个设备的主机的名称，地址和密码。<br/>8. 点击**确定**。 | 点击刚刚添加的主机，弹出该主机的子选项卡。选择**事件**，没有报告与随机数生成器相关的错误。||
| 0010028 | 将有 /dev/random 设备和 /dev/hwrng 设备的主机添加到勾选了**所需的随机数生成器的源：**中的**/dev/random** 和 **/dev/hwrng** 这两个设备的集群中。 | 验证主机不会因为随机数生成器相关问题而导致添加主机失败。 | 1. 选择**集群**选项卡，点击**新建**。<br/> 2. 弹出**新建集群**弹出框，为该集群输入**名称**；选择相应的 **CPU 类型**；在**集群版本**中选择 3.5，此时便可以对**所需的随机数生成器的源**中的设备进行勾选。<br/> 3. 勾选 **/dev/random** 和 **/dev/random**。<br/>4. 点击**确定**。<br/>5. 点击**引导操作**，弹出一个**新建集群-引导操作**弹出框。<br/>6. 点击**添加另外一个主机**，弹出**新建主机**。<br/>7. 输入已准备好的有 /dev/random 和 /dev/hwrng 两个设备的主机的名称，地址和密码。<br/>8. 点击**确定**。 | 点击刚刚添加的主机，弹出该主机的子选项卡。选择**事件**，没有报告与随机数生成>器相关的错误。| 
| 0010029 | 将有 /dev/random 设备和 /dev/hwrng 这两个设备的主机添加到勾选了**所需的随机数生成器的源：**中的 **/dev/random** 设备的集群中。 | 验证主机不会因为随机数生成器相关问题而导致添加失败的。 | 1. 选择**集群**选项卡，点击**新建**。<br/> 2. 弹出**新建集群**弹出框，为该集群输入**名称**；选择相应的 **CPU 类型**；在**集群版本**中选择 3.5，此时便可以对**所需的随机数生成器的源**中的设备进行勾选。<br/> 3. 只勾选 **dev/random** 这一个设备。<br/>4. 点击**确定**。<br/>5. 点击**引导操作**，弹出一个**新建集群-引导操作**弹出框。<br/>6. 点击**添加另外一个主机**，弹出**新建主机**。<br/>7. 输入已准备好的有 /dev/random 和 /dev/hwrng 设备的主机的名称，地址和密码。<br/>8. 点击**确定**。 | 点击刚刚添加的主机，弹出该主机的子选项卡。选择**事件**，没有报告与随机数生成器相关的错误。 |
| 0010030 | 将既没有 /dev/random 设备亦没有 /dev/hwrng 设备的主机添加到仅勾选了**所需的随机数生成器的源：**中的 **/dev/random** 这个设备的集群中。 | 验证主机会因为随机数生成器相关问题而导致添加失败。 | 1. 选择**集群**选项卡，点击**新建**。<br/> 2. 弹出**新建集群**弹出框，为该集群输入**名称**；选择相应的 **CPU 类型**；在**集群版本**中选择 3.5，此时便可以对**所需的随机数生成器的源**中的设备进行勾选。<br/> 3. 勾选 **/dev/random**。<br/>4. 点击**确定**。<br/>5. 点击**引导操作**，弹出一个**新建集群-引导操作**弹出框。<br/>6. 点击**添加另外一个主机**，弹出**新建主机**。<br/>7. 输入已准备好的没有 /dev/random 设备和 /dev/hwrng 设备的主机的 **名称**，地址和密码。<br/>8. 点击**确定**。 | 点击刚刚添加的主机，弹出该主机的子选项卡。选择**事件**，报告与随机数生成器相关的错误：Host xxx does not comply with the cluster xxx Random Number Generator sources. The Hosts supported sources are:<UNKNOWN>; and the cluster requirements are: RANDOM. |
| 0010031 | 将既没有 /dev/random 设备亦没有 /dev/hwrng 设备的主机添加到仅勾选了**所需的随机数生成器的源：**中的 **/dev/hwrng** 这个设备的集群中。 | 验证主机会因为随机数生成器相关问题而导致添加失败。 | 1. 选择**集群**选项卡，点击**新建**。<br/> 2. 弹出**新建集群**弹出框，为该集群输入**名称**；选择相应的 **CPU 类型**；在**集群版本**中选>择 3.5，此时便可以对**所需的随机数生成器的源**中的设备进行勾选。<br/> 3. 勾选 **/dev/hwrng**。<br/>4. 点击**确定**。<br/>5. 点击**引导操作**，弹出一个**新建集群-引导操>作**弹出框。<br/>6. 点击**添加另外一个主机**，弹出**新建主机**。<br/>7. 输入已准备好的没有 /dev/random 设备和 /dev/hwrng 设备的主机的 **名称**，地址和密码。<br/>8. 点击**确定**。 | 点击刚刚添加的主机，弹出该主机的子选项卡。选择**事件**，报告与随机数生成器相关的错误：Host xxx does not comply with the cluster xxx Random Number Generator sources. The Hosts supported sources are:<UNKNOWN>; and the cluster requirements are: HWRNG.  |
| 0010032 | 将既没有 /dev/random 设备亦没有 /dev/hwrng 设备的主机添加到仅勾选了**所需的随机数生成器的源：**中的 **/dev/random** 和 **/dev/hwrng** 这两个设备的集群中。 | 验证主机会因为随机数生成器相关问题而导致添加失败。 | 1. 选择**集群**选项卡，点击**新建**。<br/> 2. 弹出**新建集群**弹出框，为该集群输入**名称**；选择相应的 **CPU 类型**；在**集群版本**中选择 3.5，此时便可以对**所需的随机数生成器的源**中的设备进行勾选。<br/> 3. 勾选 **/dev/random** 和 **/dev/hwrng**。<br/>4. 点击**确定**。<br/>5. 点击**引导操作**，弹出一个**新建集群-引导操作**弹出框。<br/>6. 点击**添加另外一个主机**，弹出**新建主机**。<br/>7. 输入已准备好的没有 /dev/random 设备和 /dev/hwrng 设备的主机的 **名称**，地址和密码。<br/>8. 点击**确定**。 | 点击刚刚添加的主机，弹出该主机的子选项卡。选择**事件**，报告与随机数生成器相关的错误：Host xxx does not comply with the cluster xxx Random Number Generator sources. The Hosts supported sources are:<UNKNOWN>; and the cluster requirements are: RANDOM HWRNG. |
| 0010033 | 将既没有 /dev/random 设备亦没有 /dev/hwrng 设备的主机添加到没有勾选随机数生成器源的集群中。 | 验证主机是不会因为随机数生成器相关问题而添加失败的。 | 1. 选择**集群**选项卡，点击**新建**。<br/> 2. 弹出**新建集群**弹出框，为该集群输入**名称**；选择相应的 **CPU 类型**；在**集群版本**中选择 3.5，此时便可以对**所需的随机数生成器的源**中的设备进行勾选。<br/> 3. 不勾选任何一个设备。<br/>4. 点击**确定**。<br/>5. 点击**引导操作**，弹出一个**新建集群-引导操作**弹出框。<br/>6. 点击**添加另外一个主机**，弹出**新建主机**。<br/>7. 输入已准备好的没有任何随机数生成器设备的主机的名称，地址和密码。<br/>8. 点击**确定**。 | 点击刚刚添加的主机，弹出该主机的子>选项卡。选择**事件**，没有报告与随机数生成器相关的错误。|


